import { createStore } from '@myapp/shared/store';
import { } from {};
{pascalCase name}}StoreData as StoreData } from './types';

const use{{pascalCase name}}Store = createStore<StoreData>('{{kebabCase name}}', (set) => ({
  todo: '// TODO this store',
}));

export default use{{pascalCase name}}Store;