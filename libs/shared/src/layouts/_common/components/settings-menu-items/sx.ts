import type { SxStyle } from '@myapp/shared/theme';

export const root: SxStyle = {};

export const divider: SxStyle = { borderStyle: 'dashed' };

export const inputWrapper: SxStyle = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  width: '100%',
};
