import { PopoverListButton } from '@myapp/shared/components';
import { Typography } from '@mui/material';
import { styled } from '@mui/material/styles';

export const Wrapper = styled(PopoverListButton)``;

export const MenuItem = styled(Typography)`
  min-width: 180px;
  text-align: center;
`;
